/*
🔍 Stage 4: Pointers and Structures

Goal: combine pointers with structs to simulate real-world memory access.

Define a struct for Student (name, age, marks) and access fields using pointers.

Allocate memory for a struct dynamically and fill it.

Pass struct pointer to a function to update its values.

Create an array of struct pointers and print them.

Write a function that returns a pointer to a struct.

Implement linked list (Node struct with data and next pointer).

Implement insertion at beginning, end, and middle of linked list.

Implement deletion in linked list.

Reverse linked list using three pointers (prev, curr, next).

Implement doubly linked list.

Implement circular linked list.

💀 Stage 5: Advanced — Multi-level & Conceptual Mastery

Goal: use every pointer concept together and master complex references.

Implement string tokenization manually using pointers (like strtok).

Write your own memory copy function memcpy(void *dest, const void *src, size_t n).

Implement a simple version of qsort() using function pointers.

Implement a dynamic matrix (2D array) and transpose it using double pointers.

Create a dynamic jagged array using pointers to pointers.

Implement a simple stack using pointers and dynamic memory.

Implement a queue using pointers.

Simulate a file buffer using malloc() and pointer arithmetic.

Create your own string type using a struct that stores a char *data and size_t length.

🔮 Stage 6: Master-level — Pointer Puzzles & Tricky Concepts

Goal: pointer traps, pointer arrays, const pointers, and edge cases.

Demonstrate the difference between const int *, int *const, and const int *const.

Pointer to array vs array of pointers (int (*p)[5] vs int *p[5]).

Write a function that returns pointer to a static variable.

Pointer aliasing — show how modifying through one pointer affects another.

Void pointers — write a function that can print any type (int, char, float) using void * and type flag.

Simulate a mini memory allocator (allocate blocks using malloc, maintain list).

Implement a recursive linked list traversal using pointer-to-pointer.

Implement your own version of strdup() (duplicate string dynamically).

Use pointer to pointer to modify a linked list head inside a function.

Debug pointer bugs using printf and address tracing (intentionally mess up to understand segmentation faults).

*/

